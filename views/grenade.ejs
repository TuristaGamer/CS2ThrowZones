<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Grenade - <%= grenade.id %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <link rel="icon" href="/favicon-16x16.png" sizes="16x16" type="image/png">
    <link rel="icon" href="/favicon-32x32.png" sizes="32x32" type="image/png">
    <link rel="icon" href="/favicon-48x48.png" sizes="48x48" type="image/png">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="152x152">
    <link rel="icon" href="/android-chrome-192x192.png" sizes="192x192" type="image/png">
    <link rel="icon" href="/android-chrome-512x512.png" sizes="512x512" type="image/png">
    <style>
        main {
            max-width: 1200px;
            margin: auto;
            padding: 20px;
            background-color: #1a1a1a;
            border-radius: 10px;
            text-align: center; /* Centralizar conteúdo */
        }
        .video-container {
            display: flex;
            justify-content: center; /* Centraliza o vídeo horizontalmente */
            margin: 20px 0;
        }
        iframe {
            width: 1300px; /* Aumenta a largura do vídeo */
            height: 700px; /* Aumenta a altura do vídeo */
        }
        .back-button {
            position: absolute;
            top: 20px;
            left: 20px;
            background-color: #333;
            color: white;
            padding: 10px;
            border-radius: 5px;
            text-decoration: none;
            z-index: 5;
            cursor: pointer;
        }
    </style>
</head>
<body class="grenade-detail">
    <header>
        <div class="back-button" onclick="history.back()">&larr; Back</div>
        <a href="/">CS2ThrowZones</a>
    </header>
    <main>
        <h2>Grenade Details</h2>
        <p><strong>Landing Position:</strong> <%= grenade.landingPosition %></p>
        <p><strong>Throwing Position:</strong> <%= grenade.throwingPosition %></p>
        
        <!-- Embedding the YouTube video -->
        <% if (grenade.videoID) { %>
            <div class="video-container">
                <iframe src="https://www.youtube.com/embed/<%= grenade.videoID %>?autoplay=1" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            </div>
        <% } else { %>
            <p>Invalid video URL</p>
        <% } %>
    </main>
</body>
</html>
